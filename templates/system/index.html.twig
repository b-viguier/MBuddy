{% extends "base_app.html.twig" %}

{% block title %}System{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <twig:BreadcrumbItem href="{{ url('system_index') }}">
        System
    </twig:BreadcrumbItem>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-sm-3 col-lg-2 p-0">
            {% if isServerEnabled %}
            <twig:Tile
                title="Disable Server"
                href="{{ url('system_server_stop') }}"
                icon="bi-bootstrap-reboot"
            />
            {% else %}
            <twig:Tile
                title="Enable Server"
                href="{{ url('system_server_start') }}"
                icon="bi-bootstrap-reboot"
            />
            {% endif %}
        </div>

        <div class="col-sm-3 col-lg-2 p-0">
            <twig:Tile
                    title="Env: {{ app_env|upper }}"
                    href="{{ url('system_sf_env_list') }}"
                    icon="bi-box-arrow-in-right"
            />
        </div>
    </div>
{% endblock %}

{% block scripts %}
    {{ parent() }}
    <script>

    </script>
{% endblock %}