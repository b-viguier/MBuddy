{% extends "base_app.html.twig" %}

{% block title %}MBuddy Home{% endblock %}

{% block content %}
        <div class="row">
                <div class="col-sm-3 col-lg-2 p-0">
                        <twig:Tile
                                title="Presets"
                                href="{{ url('presets_list') }}"
                                icon="bi-box-arrow-in-right"

                        />
                </div>
                <div class="col-sm-3 col-lg-2 p-0">
                        <twig:Tile
                                title="Motif / Master"
                                href="{{ url('motif_master_index') }}"
                                icon="bi-box-arrow-in-right"
                        />
                </div>
                <div class="col-sm-3 col-lg-2 p-0">
                        <twig:Tile
                                title="System"
                                href="{{ url('system_index') }}"
                                icon="bi-box-arrow-in-right"
                        />
                </div>
        </div>
{% endblock %}
